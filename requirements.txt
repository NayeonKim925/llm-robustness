# The canonical dependency spec is pyproject.toml (with split extras).
# This file exists only so `pip install -r requirements.txt` works; it installs
# the package in editable mode with the CPU extras.
#
#   CPU (analysis, figures, tests):   pip install -e ".[viz,dev]"
#   GPU (training/eval/calibration):  pip install -e ".[viz,dev,gpu]"

-e .[viz,dev]
